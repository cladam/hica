// Hica — Type Checker (stub)
// Placeholder for type and effect inference.
module semantics/checker

import syntax/ast

// TODO: Implement Hindley-Milner type inference with row polymorphism.
// For now, this module provides a trivial "check" that walks the AST.

pub fun check( n : node ) : node
  n  // pass-through for now
